<UserControl x:Class="ModulWpfApp.Modul.ModulModulesStyledNew"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">
    <Grid Background="White">
        <GroupBox Header="Модули" Margin="2" Background="White">
            <Button Content="Создать модуль" 
                    Padding="10,5" 
                    Margin="5,5,5,0" 
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Top"
                    BorderBrush="Gray" 
                    BorderThickness="1" 
                    Background="Transparent" 
                    Foreground="Black" 
                    Cursor="Hand"
                    MinWidth="100"
                    Height="40"
                    MouseDoubleClick="CreateModuleButton_MouseDoubleClick"
                    >
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFE0E0E0"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="DarkGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </GroupBox>
    </Grid>
</UserControl>
